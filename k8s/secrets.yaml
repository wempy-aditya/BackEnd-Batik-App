apiVersion: v1
kind: Secret
metadata:
  name: batik-api-secrets
  namespace: batik-api
type: Opaque
stringData:
  # IMPORTANT: Change these values before deploying!
  # Generate SECRET_KEY: openssl rand -hex 32
  SECRET_KEY: "CHANGE-THIS-TO-RANDOM-64-CHAR-STRING"
  
  # Database Credentials
  POSTGRES_USER: "batik_user"
  POSTGRES_PASSWORD: "CHANGE-THIS-STRONG-PASSWORD"
  
  # Admin Credentials
  ADMIN_NAME: "Admin"
  ADMIN_EMAIL: "admin@yourdomain.com"
  ADMIN_USERNAME: "admin"
  ADMIN_PASSWORD: "CHANGE-THIS-ADMIN-PASSWORD"

---
# Alternative: Use external secret manager (recommended for production)
# Example with sealed-secrets or external-secrets-operator
# apiVersion: external-secrets.io/v1beta1
# kind: ExternalSecret
# metadata:
#   name: batik-api-secrets
#   namespace: batik-api
# spec:
#   secretStoreRef:
#     name: vault-backend
#     kind: SecretStore
#   target:
#     name: batik-api-secrets
#   data:
#   - secretKey: SECRET_KEY
#     remoteRef:
#       key: batik-api/secret-key
